# pftb — 仕様書 SPEC(索引)

> **この文書群がプロジェクトの正本(Single Source of Truth)です。**
> 実装(`index.html`)とSPECが食い違った場合、原則SPECを正とし、
> 仕様変更は「①SPECを更新 → ②実装 → ③SPECと一致を検証」の順で行う。
> 数値(確率・係数・閾値)は意図を持った決定事項なので、変更時は必ず該当ドキュメントを更新する。

- **バージョン**: アプリ **v0.1.0**(`data.js` の `VERSION`) / セーブデータ schema **v20**
  - どちらもタイトル画面の下端に出る。**手で上げる**(日付やコミットハッシュは入れない)
- **形式**: 単一HTMLファイル(`index.html`)。外部依存なし、オフライン動作。
- **想定プレイ**: スマートフォン縦持ち・1人プレイ(single play browser football game)。
- **コンセプト**: サッカーゲーム。**詳細は未確定** → [03. ゲームデザイン](docs/03-game-design.md) の「決めること」を参照。

## 仕様ドキュメント(docs/)

| # | ドキュメント | 主な内容 |
|---|---|---|
| 1 | [アーキテクチャと技術仕様](docs/01-architecture.md) | 構成・永続化・ソース分割・不変条件・ビルド |
| 2 | [データモデル](docs/02-data-model.md) | セーブ状態 `S`(schema v1)・マイグレーション |
| 3 | [ゲームデザイン](docs/03-game-design.md) | コンセプト・ゲームループ・**これから決めること** |
| 4 | [テストと検証](docs/04-testing.md) | 検証ハーネス・リリース手順 |
| 5 | [決定事項ログとバックログ](docs/05-decisions-backlog.md) | なぜその選択か・未実装候補 |
| 6 | [デザインシステム](docs/06-design-system.md) | 色/タイポ/レイアウトのトークン・画面インベントリ(正本はデザインモック) |
| 7 | [試合エンジン](docs/07-match-engine.md) | 見せかけを持たない設計・時計・判定の形・移植の進み具合 |
| 8 | [スキル](docs/08-skills.md) | 全54種(位置48 + 汎用6)の一覧・掛かり先の地図・**埋めた穴と残っている穴** |

## 変更フロー(要約)

1. 仕様を決める → 該当 `docs/` を更新(数値の意図は決定事項ログにも残す)。
2. 実装(`src/` を編集 → `python build.py` で `index.html` を再生成)。
3. 検証([04. テスト](docs/04-testing.md))→ SPECと実装の一致を確認。
4. コミット(日本語メッセージ)。プッシュは明示指示時のみ。

## 姉妹プロジェクト card-eleven

本プロジェクトは [card-eleven](https://github.com/toomorirh/card-eleven)(SFCレトロ風の収集型カードサッカー)の
**土台(単一HTML・build.py・SPEC/docs体制・テストハーネス)だけ**を引き継いで新規に始めたもの。
ゲームロジックとアセットは引き継いでいない。試合エンジンやガチャなど、card-eleven に既存の実装が
参考になる場面では、**その都度ローカルの `c:\Claude\repository\card-eleven` を参照して必要な考え方だけ取り込む**
(丸ごとコピーはしない)。取り込んだ場合は決定事項ログにその旨を残す。
